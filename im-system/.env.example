# IM System 环境变量配置示例
# 复制此文件为 .env 并修改配置值

# ========================
# 服务配置
# ========================
NODE_ID=node1

# ========================
# MySQL 配置
# ========================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=password
MYSQL_DATABASE=im_db

# ========================
# Redis 配置
# ========================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ========================
# MongoDB 配置
# ========================
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=im_db

# ========================
# MinIO 文件存储配置
# ========================
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=im-files
MINIO_USE_SSL=false

# ========================
# JWT 认证配置
# ========================
JWT_SECRET=im-system-jwt-secret-key-change-in-production

# ========================
# 命令行参数参考 (可选)
# ========================
# --host          服务监听地址 (默认: 0.0.0.0)
# --port          服务端口 (默认: 8080)
# --node-id       节点ID (默认: node1)
# --mysql-host    MySQL地址
# --mysql-port    MySQL端口
# --mysql-user    MySQL用户名
# --mysql-password MySQL密码
# --mysql-database MySQL数据库名
# --redis-host    Redis地址
# --redis-port    Redis端口
# --redis-password Redis密码
# --mongo-uri     MongoDB连接URI
# --mongo-database MongoDB数据库名
# --minio-endpoint MinIO地址
# --minio-access-key MinIO访问密钥
# --minio-secret-key MinIO私密密钥
# --minio-bucket  MinIO存储桶
# --metrics-port  Prometheus指标端口 (默认: 9090)
